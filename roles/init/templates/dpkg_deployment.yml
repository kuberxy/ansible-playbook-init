---

- name: copy packages
  copy: src={{ "{{" }} local_download_dir {{ "}}" }}/{{ "{{" }} item {{ "}}" }}.deb dest={{ "{{" }} image_dir {{ "}}" }} owner={{ "{{" }} elang_user {{ "}}" }} group={{ "{{" }} elang_user {{ "}}" }} mode=0755
  loop: "{{ "{{" }} dpkg_packages {{ "}}" }}"

- name: install packages
  shell: "apt-get install -y ./*.deb"
  args:
    chdir: "{{ "{{" }} image_dir {{ "}}" }}"
